#include <bits/stdc++.h>
using namespace std;

void explainMultimap() {

	// everything same as map, 
	// but it can store multiple or duplicate keys

	// mpp[key] cannot be used here 
	
}

int main(){

	explainMultimap();
	
    return 0;
}