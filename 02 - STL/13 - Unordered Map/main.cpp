#include <bits/stdc++.h>
using namespace std;


void explainUnorderedMap() {
	
    // store unique keys

    // it does not store elements in a sorted order.
    // Elements are stored in a random order

    // Time complexity : O(1) almost everytime
}


int main(){

    explainUnorderedMap();

    return 0;
}